spring:
  application:
    name: university

  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://localhost:3306/university?useUnicode=true&characterEncoding=utf8
    username: root
    password: aa456456!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none  # ??? ?? ?? ??/???? ??
    properties:
      hibernate.default_schema: university
      hibernate.format_sql: true
      hibernate.show_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect